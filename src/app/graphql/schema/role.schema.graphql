scalar Date

type Role {
  _id: ID!
  name: String!
  createdAt: Date!
  updatedAt: Date
}

type Query {
  roles: [Role]
}

type Mutation {
  "Store Role"
  storeRole(role: RoleInput): Role!
}

input RoleInput {
  name: String!
  createdAt: Date
  updatedAt: Date
}